# Results

Generated modeling outputs are separated by experiment:

```text
resnet_frozen/
resnet_layer4/
cnn/
```

The older `resnet/` folder may contain earlier frozen-ResNet outputs. Keep it as
historical output unless the exact run configuration is known.

Model checkpoints and large generated files should generally not be committed
unless they are intentionally needed.

