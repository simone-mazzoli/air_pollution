# Archive

This folder keeps older Sensor.Community modeling code from an earlier project
direction.

It is retained for reproducibility and history, but it is not part of the
current EEA-reference training pipeline in `06_models/`.

Some archived folders contain their own notes about the older experiments.

